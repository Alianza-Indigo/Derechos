name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: derechos
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      DATABASE_URL: postgres://postgres:postgres@localhost:5432/derechos
      NEXTAUTH_SECRET: ci-secret-para-pruebas
      NEXTAUTH_URL: http://localhost:3000
      DEMO_PASSWORD: demo-seguro

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npm run lint
      - run: npm run db:migrate
      - run: npm run db:seed
      # `next build` genera .next/types (LayoutProps/PageProps) que consume tsc.
      - run: npm run build
      - run: npm run typecheck
      - run: npm run test
